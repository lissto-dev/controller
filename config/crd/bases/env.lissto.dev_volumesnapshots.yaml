---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: volumesnapshots.env.lissto.dev
spec:
  group: env.lissto.dev
  names:
    kind: VolumeSnapshot
    listKind: VolumeSnapshotList
    plural: volumesnapshots
    singular: volumesnapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.volumeIdentifier.serviceName
      name: Service
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.size
      name: Size
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VolumeSnapshot is the Schema for the volumesnapshots API.
          It represents a point-in-time snapshot of a volume stored in object storage.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VolumeSnapshot
            properties:
              volumeIdentifier:
                description: VolumeIdentifier uniquely identifies the volume for restore
                  matching
                properties:
                  image:
                    description: Image used by the service (e.g., postgres:15)
                    type: string
                  mountPath:
                    description: MountPath inside the container (e.g., /var/lib/postgresql/data)
                    type: string
                  repo:
                    description: Repo is the source repository URL (from blueprint)
                    type: string
                  serviceName:
                    description: ServiceName from docker-compose
                    type: string
                required:
                - image
                - mountPath
                - repo
                - serviceName
                type: object
            required:
            - volumeIdentifier
            type: object
          status:
            description: status defines the observed state of VolumeSnapshot
            properties:
              checksum:
                description: Checksum is the SHA256 checksum of the snapshot archive
                type: string
              completionTime:
                description: CompletionTime is when the snapshot completed
                format: date-time
                type: string
              jobName:
                description: JobName is the name of the Job that created this snapshot
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              phase:
                description: Phase represents the current phase of the snapshot
                type: string
              size:
                description: Size is the size of the snapshot (e.g., "1Gi")
                type: string
              startTime:
                description: StartTime is when the snapshot started
                format: date-time
                type: string
              storagePath:
                description: |-
                  StoragePath is the path in object storage where the snapshot is stored
                  Format: {user}/{env}/{repo-hash}/{service}/{mountPath-hash}/{snapshot-id}.tar.gz
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
